# Videopac TODO

## Unwired infrastructure (audit 2026-03-18)
Code that exists but is never called from main code paths.

- [ ] Wire up emulation speed — ConfigManager has get/set, nothing reads it
- [ ] Wire up screenshot format — always PPM, setting ignored
- [ ] Wire up audio buffer size — menu saves it, init_audio() never reads it
- [ ] Wire up physical joystick input — InputMapper has detect/mapping, frontend ignores it
- [ ] Wire up OSD customization — notification position, font size, opacity all persisted but never applied
- [ ] Remove or wire up MessageDialog — instantiated, never shown
- [ ] Remove ProgressDialog and ConfirmDialog — defined, never instantiated
- [ ] Remove unused OSD render methods (render_notification, render_fps, render_status_indicator)

## UI features
- [ ] Add input remapping GUI — currently keyboard/joystick mappings can only be changed by editing config.ini manually
